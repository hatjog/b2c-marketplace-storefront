PAYLOAD_API_URL=http://localhost:9003
NEXT_PUBLIC_PAYLOAD_MARKET_ID=
MEDUSA_BACKEND_URL=http://localhost:9002
# Optional dedicated secret for voucher claim HMAC binding.
# If unset, the server action falls back to JWT_SECRET.
GP_VOUCHER_CLAIM_HMAC_SECRET=
# MEDUSA_URL=http://localhost:9002
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
NEXT_PUBLIC_BASE_URL=http://localhost:3001
NEXT_PUBLIC_DEFAULT_REGION=pl
NEXT_PUBLIC_STRIPE_KEY=supersecret
# v1.9.0 wf5 (closes CC-1 F-CC1-013): per-market Stripe publishable key.
# Preferred form is per-market `NEXT_PUBLIC_STRIPE_KEY_<MARKET_UPPER>`;
# the lookup falls back to `NEXT_PUBLIC_STRIPE_KEY` only when per-market
# is absent. Add one per Stripe-enabled market.
NEXT_PUBLIC_STRIPE_KEY_BONBEAUTY=pk_test_REPLACE_ME
# CSP enforce/report-only toggle (storefront/lib/security/csp.ts).
STOREFRONT_CSP_MODE=enforce
# Storefront talks to this backend origin; CSP `connect-src` includes it.
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9002
REVALIDATE_SECRET=supersecret
NEXT_PUBLIC_SITE_NAME="Fleek Marketplace"
NEXT_PUBLIC_SITE_DESCRIPTION="Fleek Markeplace"
NEXT_PUBLIC_ALGOLIA_ID=supersecret
NEXT_PUBLIC_ALGOLIA_SEARCH_KEY=supersecret
NEXT_PUBLIC_VENDOR_URL=http://localhost:5174
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN= # CI/CD only; never commit real value
STOREFRONT_BASE_URL=http://localhost:3001
# Story 4.2: publiczny klucz MapTiler tylko dla client-side fallback; hardening proxy po stronie serwera zostaje dla Story 4.4/4.5.
NEXT_PUBLIC_MAPTILER_BONBEAUTY_STYLE_ID=streets-v2
NEXT_PUBLIC_MAPTILER_API_KEY=
